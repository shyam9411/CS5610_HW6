<h1>Edit Task</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.task_path(@conn, :update, @task)) |> Map.put(:edit, true) %>

<span><%= link "Back", to: Routes.manager_path(@conn, :index) %></span>
